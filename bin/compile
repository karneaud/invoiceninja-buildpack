#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

# install invoiceninja from github 
echo "Installing Invoice Ninja"
wget -qO- -O tmp.zip https://github.com/invoiceninja/invoiceninja/archive/master.zip && unzip tmp.zip -d app && rm tmp.zip
echo "Downloaded Invoice Ninja"
