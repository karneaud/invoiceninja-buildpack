#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

# install invoiceninja from github 
wget https://github.com/invoiceninja/invoiceninja/archive/master.zip ${BUILD_DIR}/app
# copy over the env
cp .env ${BUILD_DIR}/app/.env
echo "Downloaded Invoice Ninja"
