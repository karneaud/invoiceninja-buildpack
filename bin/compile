#!/usr/bin/env bash
# bin/compile <build-dir> <cache-dir>

# install invoiceninja from github 
echo "Installing Invoice Ninja to ${BUILD_DIR}"
unzip /tmp/*.zip -C ${BUILD_DIR}/app < <(wget -q -O - https://github.com/invoiceninja/invoiceninja/archive/master.zip)
echo "Downloaded Invoice Ninja"
